import React, { useContext, useState } from 'react';
//import { useMutation, useQuery } from '@apollo/react-hooks';

function Quiz() {

    return (
        <div>
            Quiz Page
        </div>
    );
}
export default Quiz;